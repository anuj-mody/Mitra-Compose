<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.NewMitraApp" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <item name="statusBarBackground">@color/primary</item>
        <item name="android:statusBarColor" tools:targetApi="l">?attr/statusBarBackground</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.NewMitraApp.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.NewMitraApp.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.NewMitraApp.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>